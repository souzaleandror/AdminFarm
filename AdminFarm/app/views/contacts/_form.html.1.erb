<%= form_with(model: contact, local: true) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :contact_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :contact_email %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :contact_phone %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message, id: :contact_message %>
  </div>

  <div class="field">
    <%= form.label :accept %>
    <%= form.check_box :accept, id: :contact_accept %>
  </div>

  <div class="field">
    <%= form.label :newsletter %>
    <%= form.check_box :newsletter, id: :contact_newsletter %>
  </div>

  <div class="field">
    <%= form.label :info %>
    <%= form.text_field :info, id: :contact_info %>
  </div>

  <div class="field">
    <%= form.label :obs %>
    <%= form.text_field :obs, id: :contact_obs %>
  </div>

  <div class="field">
    <%= form.label :answered %>
    <%= form.check_box :answered, id: :contact_answered %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
